@page
@model SelfServiceHrProject.Pages.Position.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>Positions</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Positions.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Positions.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Positions.Code)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Positions.Code)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="Positions.Id" />
        <input type="submit" value="Delete" class="btn btn-outline-danger" />
        <a asp-page="./Index" class="btn btn-outline-primary">Back</a>
    </form>
</div>
